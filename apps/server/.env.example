# Server host/port
HOST=0.0.0.0
PORT=8080

# Allowed CORS origin (the client dev server)
CORS_ORIGIN=http://localhost:3000

# Route prefix where Swagger UI is served
SWAGGER_UI_PREFIX=/swagger-ui

NODE_ENV=development
